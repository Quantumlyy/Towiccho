apiVersion: v1
kind: Service
metadata:
  name: {{ template "towicchoserver.fullname" . }}
  labels:
    app: {{ template "towicchoserver.name" . }}
    chart: {{ template "towicchoserver.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "towicchoserver.name" . }}
    release: {{ .Release.Name }}
